shared:
  image: node:20
  steps:
    - echo: echo hoge
  environment:
    SD_SKIP_REPOSITORY_CLONE: true

jobs:
  main:
    requires: [ ~commit ]
    annotations:
      screwdriver.cd/virtualJob: true

  hub-a:
    requires: [ ~main ]

  hub-b:
    requires: [ ~main ]

  hub-c:
    requires: [ ~main ]
