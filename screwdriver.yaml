shared:
  image: node:22
  steps:
    - echo: echo hoge

jobs:
  normal:
    requires: [ ~commit, ~pr ]

  prclosed:
    requires: [ ~pr-closed ]

  prclosed-branch:
    requires: [ ~pr-closed:specific ]
