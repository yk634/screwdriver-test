shared:
  image: node:22
  steps:
    - echo: echo hoge

jobs:
  a:
    requires: []
  b:
    requires: [ ~a ]
  c:
    requires: []
  d:
    requires: [ ~c ]

stages:
  foo:
    jobs: [ a, b ]
    requires: [ ~commit ]
    sourcePaths: [ "README.md", "fuga.txt" ]
  bar:
    jobs: [ c, d ]
    requires: [ ~commit ]
    sourcePaths: [ "README.md", "fuga.txt" ]
    setup:
      sourcePaths: [ "!README.md", "!fuga.txt" ]
